// 自定义配置，参考 ./default/setting.config.js，需要自定义的属性在这里配置即可
module.exports = {
  theme: {
    color: '#13c2c2',
    mode: 'light',
  },
  layout: 'head',
  pageWidth: 'fluid',                   //内容区域宽度，fixed:固定宽度，fluid:流式宽度
  multiPage: false,
  animate: {
    name: 'lightSpeed',
    direction: 'left'
  }
}
